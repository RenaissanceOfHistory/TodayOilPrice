server:
  port: 8090

spring:
  application:
    name: provider-file
  cloud:
    nacos:  # Nacos
      discovery:
        group: todayOil
        server-addr: ${prod.host}:8848


prod:
  host: 192.168.227.134